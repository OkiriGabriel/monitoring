global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'riders-api-monitor'

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files:
  - 'alerts.yml'

scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  # Blackbox exporter for API monitoring
  - job_name: 'blackbox'
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          - https://rider-api.fezdelivery.co
          # - https://rider-api.fezdelivery.co/health
          # - https://your-riders-api.com/api/riders/status
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: blackbox:9115

  # Blackbox exporter metrics endpoint
  - job_name: 'blackbox_exporter'
    static_configs:
      - targets: ['blackbox:9115']

  # Node exporter for system metrics
  - job_name: 'node'
    static_configs:
      - targets: ['node-exporter:9100']

  # Your Riders API metrics endpoint (if you have one)
  - job_name: 'riders_api'
    static_configs:
      - targets: ['your-riders-api:8080']
    metrics_path: '/metrics'
